/* ============================================================
   Página: Inicio
   ============================================================ */

export default function Home() {
  return (
    <div>
      <h1>🏠 Inicio</h1>
      <p>Bienvenido a la app de ejemplo de React Router.</p>
      <p>Esta es una SPA (Single Page Application). Las "páginas"
        no son archivos HTML separados — son componentes que React
        renderiza según la URL.</p>
      <p>Navegá por los links de arriba. NOTAR que NO hay recarga
        de página. Todo es instantáneo.</p>
    </div>
  );
}
